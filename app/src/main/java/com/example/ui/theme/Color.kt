package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Colors
val PrimaryBlue = Color(0xFF1D87E4)       // Beautiful soft Telegram Blue
val SecondaryBlue = Color(0xFF2E6EEF)     // Accent blue
val LightBackground = Color(0xFFF1F5F9)   // Warm clean slate-gray white
val LightSurface = Color(0xFFFFFFFF)      // Pure white for cards
val LightText = Color(0xFF1E293B)         // Dark slate for readability

// Dark Colors
val DarkBackground = Color(0xFF0F172A)    // Elegant deep slate space blue
val DarkSurface = Color(0xFF1E293B)       // Rich card color
val DarkText = Color(0xFFF8FAFC)          // Soft clean white

// Ping Status Indicators
val PingExcellent = Color(0xFF10B981)     // Smooth soft green
val PingGood = Color(0xFFF59E0B)          // Smooth soft amber
val PingPoor = Color(0xFFEF4444)          // Smooth soft red
val PingOffline = Color(0xFF64748B)       // Gray
